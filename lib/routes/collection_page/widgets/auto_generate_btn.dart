import 'package:flutter/material.dart';

class AutoGenerateBtn extends StatelessWidget {
  const AutoGenerateBtn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
