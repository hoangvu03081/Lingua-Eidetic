import 'package:flutter/material.dart';

class PreviewSharePage extends StatelessWidget {
  const PreviewSharePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
