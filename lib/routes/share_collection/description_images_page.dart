import 'package:flutter/material.dart';

class DescriptionImagesPage extends StatelessWidget {
  const DescriptionImagesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
